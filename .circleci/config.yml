version: 2.1

jobs:
  build:
    docker:
      - image: cimg/base:stable
    resource_class: arm.medium
    steps:
      - checkout
      - run:
          name: Run job_1
          command: echo "This is job_1 aaa"